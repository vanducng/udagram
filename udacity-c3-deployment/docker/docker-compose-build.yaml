version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: vanducng/reverseproxy 
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: vanducng/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed
    image: vanducng/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend
    image: vanducng/udacity-frontend:local
